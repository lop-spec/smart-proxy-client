{
  "name": "smart-proxy-client",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portable Smart Proxy client for Windows",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "prepare:runtime": "node scripts/prepare-runtime.mjs",
    "build:portable": "npm run prepare:runtime && neu build --embed-resources",
    "verify:portable": "node scripts/verify-portable.mjs"
  },
  "devDependencies": {
    "@neutralinojs/neu": "11.7.2",
    "adm-zip": "0.5.16"
  },
  "engines": {
    "node": ">=22"
  }
}
